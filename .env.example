# ─── Required ────────────────────────────────────────────────────────────────
WEBHOOK_SECRET=change-me
SESSION_SECRET=change-me

# ─── LLM — default: local Ollama ─────────────────────────────────────────────
LLM_PROVIDER=ollama
LLM_BASE_URL=http://ollama:11434/v1
LLM_MODEL=qwen3.5:397b-cloud

# ─── Embeddings ───────────────────────────────────────────────────────────────
# Provider: ollama | openai | google | http
EMBEDDING_PROVIDER=ollama

# ── Option A: Ollama (default, CPU-friendly) ──────────────────────────────────
# Best: qwen3-embedding:0.6b  — 639MB, 40K ctx, code-aware, #1 MTEB multilingual
# Also: nomic-embed-text      — 274MB, fast, 768-dim
# Also: embeddinggemma        — 622MB, 300M params, but only 2K ctx (avoid for code)
EMBEDDING_MODEL=qwen3-embedding:0.6b
# EMBEDDING_BASE_URL=http://localhost:11434   # override if Ollama is remote

# ── Option B: OpenAI ──────────────────────────────────────────────────────────
# EMBEDDING_PROVIDER=openai
# EMBEDDING_MODEL=text-embedding-3-small     # 1536-dim, $0.02/1M tokens
# EMBEDDING_API_KEY=sk-...

# ── Option C: Google (free tier: 1500 RPM) ────────────────────────────────────
# EMBEDDING_PROVIDER=google
# EMBEDDING_MODEL=text-embedding-004         # 768-dim, CODE_RETRIEVAL_QUERY task
# EMBEDDING_API_KEY=AIza...

# ── Option D: Any OpenAI-compatible endpoint (Cohere, Together, Voyage, Azure) ─
# EMBEDDING_PROVIDER=http
# EMBEDDING_BASE_URL=https://api.cohere.com/compatibility/v1
# EMBEDDING_MODEL=embed-v4.0
# EMBEDDING_API_KEY=...

# NOTE: Anthropic/Claude has NO embedding API. Use any of the above.

# ─── Review depth ─────────────────────────────────────────────────────────────
# fast     — 1-hop graph traversal, no embeddings
# standard — 2-hop graph traversal, no embeddings (default)
# deep     — 2-hop + semantic neighbors via embedding search
REVIEW_DEPTH=standard

# ─── Optional: cloud LLM ─────────────────────────────────────────────────────
# LLM_PROVIDER=openai
# LLM_API_KEY=sk-...
# LLM_MODEL=gpt-4o-mini

# ─── Optional: Azure OpenAI ──────────────────────────────────────────────────
# LLM_PROVIDER=azure
# LLM_BASE_URL=https://your-resource.openai.azure.com/openai/deployments/gpt-4o
# LLM_API_KEY=...

# ─── VCS tokens ───────────────────────────────────────────────────────────────
GITHUB_TOKEN=ghp_...
AZURE_DEVOPS_TOKEN=
